[package]
name = "cmux-tmux"
version = "0.0.1"
edition = "2021"
license = "MIT"

[lib]
name = "cmux_tmux"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
